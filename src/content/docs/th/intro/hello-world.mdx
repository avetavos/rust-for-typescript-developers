---
title: "Hello, World — โปรแกรม Rust แรกของคุณ"
description: 'fn main, println!, cargo run, และสิ่งที่ทำให้ Hello world ของ Rust แตกต่างจาก TypeScript'
---

import TsGo from '../../../../components/TsGo.astro';
import Playground from '../../../../components/Playground.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import RsOnly from '../../../../components/RsOnly.astro';

## โปรแกรมที่ทุกภาษาเริ่มต้น

การเขียน "Hello, world!" คือพิธีกรรมแรกในทุกภาษาใหม่ ใน TypeScript คุณใช้ `console.log` ส่วนใน Rust ใช้ `println!` สองตัวนี้หน้าตาคล้ายกัน แต่เบื้องหลังทำงานต่างกันมาก

## เปรียบเทียบแบบเคียงข้างกัน

export const helloTs = `// TypeScript hello world
const greeting: string = "Hello, world!";
console.log(greeting);

// Template literals
const name = "TypeScript developer";
console.log(\`Welcome, \${name}!\`);`;

export const helloRs = `fn main() {
    let greeting: &str = "Hello, world!";
    println!("{greeting}");

    // Named argument in format string
    let name = "TypeScript developer";
    println!("Welcome, {name}!");
}`;

<TsGo ts={helloTs} go={helloRs} />

มีหลายสิ่งที่ควรสังเกตทันที:

- **`fn main()`** — ทุก Rust binary เริ่มต้นที่นี่ ไม่มี Module system หรือ Top-level execution แบบ Node.js; `main` คือ Entry point ที่จำเป็น
- **`println!`** — เครื่องหมายอัศเจรีย์บอกว่านี่คือ **Macro** ไม่ใช่ Function ตัว Compiler จะตรวจ Format string ตั้งแต่ Compile time แล้วแทน `{greeting}` ด้วยค่าของตัวแปร `greeting` คล้ายกับ Template literal แต่ตรวจตั้งแต่ Compile time ไม่ใช่ Runtime
- **`let greeting: &str`** — `&str` คือ String slice หรือ Reference ไปยังลำดับ UTF-8 bytes มองว่าเป็น Immutable view ของ string ก็ได้ ส่วน string type ที่ Owned และอยู่บน Heap คือ `String`
- **Semicolons** — Rust ใช้ Semicolons เพื่อจบ Statements เหมือนกับ TypeScript

## การจัดรูปแบบด้วย println!

`println!` มีความสามารถมากกว่า `console.log` Format string ใช้ `{}` เป็น Placeholder และคุณสามารถอ้างอิงตัวแปรด้วยชื่อ (`{name}`) หรือใช้ Positional placeholders:

```rust
// @skip-verify nocode: fragment illustrating println! placeholder styles (`value` from surrounding context)
println!("{}", value);           // positional
println!("{value}");             // named (Rust 1.58+)
println!("{:?}", value);         // debug format — works on most types
println!("{:#?}", value);        // pretty debug format
println!("{:.2}", 3.14159);      // 3.14 — two decimal places
```

## การรันโปรแกรม

หลังจาก Scaffold ด้วย `cargo new hello-world` ไฟล์ `src/main.rs` จะมี:

```rust
fn main() {
    println!("Hello, world!");
}
```

สั่งรันด้วย:

```bash
cargo run
```

คุณจะเห็น:

```text
   Compiling hello-world v0.1.0
    Finished dev [unoptimized + debuginfo] target(s) in 0.42s
     Running `target/debug/hello-world`
Hello, world!
```

การรันครั้งแรก Compile; การรันครั้งถัดไปจะ Compile เฉพาะไฟล์ที่เปลี่ยนแปลง — คล้ายกับการ Compile TypeScript แบบ Incremental

## ลองทดสอบ

รันโปรแกรม Rust ในเบราว์เซอร์ของคุณตอนนี้เลย ไม่ต้องติดตั้งอะไร

export const helloCode = `fn main() {
    println!("Hello, world!");

    let name = "TypeScript developer";
    println!("Welcome, {name}!");

    // Debug formatting with {:?}
    let numbers = [1, 2, 3, 4, 5];
    println!("numbers = {numbers:?}");

    // Formatted floats
    let pi = 3.14159;
    println!("pi = {pi:.2}");
}`;

<Playground code={helloCode} />

<RsOnly title="println! คือ Macro ไม่ใช่ Function">

`println!` คือ **Macro** — สังเกตได้จากเครื่องหมาย `!` ใน Rust Macro คือรูปแบบหนึ่งของ Metaprogramming: Compiler จะ Expand ออกเป็นโค้ดจริงก่อนขั้นตอนสร้างโค้ด ตัว `println!` จะวิเคราะห์ Format string ตั้งแต่ Compile time และยืนยันว่า Argument type ตรงกับ Placeholder ถ้าคุณเขียน `println!("{}", name)` แต่ `name` Print ไม่ได้ ตัว Compiler จะฟ้องตั้งแต่ Compile time

นี่แตกต่างโดยพื้นฐานจาก `console.log` ใน JavaScript ซึ่งรับค่าใดก็ได้และเรียก `.toString()` ขณะ Runtime ด้วย `println!` Format errors คือ Compile errors

Macros มาตรฐานอื่นที่จะใช้บ่อย:
- `print!` — เหมือน `println!` แต่ไม่มี Newline ท้าย
- `format!` — Return `String` แทนที่จะ Print
- `eprintln!` — Print ไปยัง `stderr`
- `dbg!` — Print ทั้งค่าและ Expression ที่ให้ค่านั้น ใช้ตอน Debug

</RsOnly>

export const quizHello = [
  {
    q: "เครื่องหมาย `!` หลัง `println` บ่งบอกอะไร?",
    options: [
      "เป็น Logical NOT operator ที่ใช้กับผลลัพธ์",
      "ทำเครื่องหมายว่า println เป็น Unsafe operation",
      "บ่งบอกว่า println คือ Macro ไม่ใช่ Function",
      "เป็น Syntax บังคับสำหรับคำสั่ง Print ทุกแบบใน Rust"
    ],
    answer: 2
  },
  {
    q: "Function Entry point ใน Rust binary program คืออะไร?",
    options: [
      "start()",
      "run()",
      "init()",
      "main()"
    ],
    answer: 3
  },
  {
    q: "Format specifier ใดที่ Print ค่าโดยใช้ Debug formatting ใน println!?",
    options: [
      "{}",
      "{:?}",
      "{:debug}",
      "{!}"
    ],
    answer: 1
  },
  {
    q: "คำสั่งใด Compile และรัน Rust project ทันที?",
    options: [
      "rustc main.rs && ./main",
      "cargo run",
      "cargo exec",
      "rust run src/main.rs"
    ],
    answer: 1
  }
];

<Quiz client:visible id="intro/hello-world" questions={quizHello} />

<ProgressTracker client:visible id="intro/hello-world" />
