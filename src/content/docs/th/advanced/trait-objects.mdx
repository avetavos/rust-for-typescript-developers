---
title: 'Trait Objects'
description: 'Dynamic dispatch ด้วย `dyn Trait` และ `Box<dyn Trait>`, static vs dynamic dispatch เทียบกับ TypeScript interfaces และ polymorphism'
---

import TsGo from '../../../../components/TsGo.astro';
import Playground from '../../../../components/Playground.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import RsOnly from '../../../../components/RsOnly.astro';

## TypeScript interfaces vs Rust trait objects

TypeScript interfaces เปิดใช้งาน runtime polymorphism โดยไม่ต้องทำอะไรพิเศษ — คุณส่ง object ใดก็ได้ที่ตรงกับ shape ของ interface คู่เทียบของ Rust คือ **trait object**: ค่าที่นำหน้าด้วย `dyn` ซึ่งบอก compiler ให้ใช้ **vtable** สำหรับ dynamic dispatch แทนการ monomorphise

คุณมีสองทางเลือกใน Rust:

| วิธี | Syntax | Dispatch | ใช้เมื่อ |
|---|---|---|---|
| Generic with bound | `fn foo<T: Animal>(a: T)` | Static (monomorphised) | เมื่อรู้ทุก type ตอน compile time |
| Trait object | `fn foo(a: &dyn Animal)` | Dynamic (vtable) | เมื่อ concrete type ไม่รู้ตอน compile time |

export const dynTs = `// TypeScript — interface polymorphism
interface Animal {
  speak(): string;
  name(): string;
}

class Dog implements Animal {
  speak() { return "Woof"; }
  name() { return "Rex"; }
}

class Cat implements Animal {
  speak() { return "Meow"; }
  name() { return "Whiskers"; }
}

function makeNoise(animal: Animal): void {
  console.log(\`\${animal.name()} says \${animal.speak()}\`);
}

const animals: Animal[] = [new Dog(), new Cat()];
animals.forEach(makeNoise);`;

export const dynRs = `trait Animal {
    fn speak(&self) -> &str;
    fn name(&self) -> &str;
}

struct Dog { name: String }
struct Cat { name: String }

impl Animal for Dog {
    fn speak(&self) -> &str { "Woof" }
    fn name(&self) -> &str { &self.name }
}
impl Animal for Cat {
    fn speak(&self) -> &str { "Meow" }
    fn name(&self) -> &str { &self.name }
}

// &dyn Animal = reference to any type implementing Animal
fn make_noise(animal: &dyn Animal) {
    println!("{} says {}", animal.name(), animal.speak());
}

fn main() {
    // Vec of boxed trait objects — types can differ at runtime
    let animals: Vec<Box<dyn Animal>> = vec![
        Box::new(Dog { name: String::from("Rex") }),
        Box::new(Cat { name: String::from("Whiskers") }),
    ];
    for a in &animals {
        make_noise(a.as_ref());
    }
}`;

<TsGo ts={dynTs} go={dynRs} />

## `Box<dyn Trait>` — heap-allocated trait objects

เมื่อต้องการเก็บ trait object ไว้ใน collection (เช่น `Vec`) หรือ return ออกจาก function คุณต้อง box ก่อน ตัว `Box<dyn Trait>` เป็น fat pointer: เก็บทั้ง address ของข้อมูลบน heap และ pointer ไปยัง vtable

```rust
// @skip-verify fragment: builds on the Animal/Dog/Cat example above (dynRs), not standalone
// Returning a trait object from a function
fn new_animal(kind: &str) -> Box<dyn Animal> {
    match kind {
        "dog" => Box::new(Dog { name: String::from("Rex") }),
        _     => Box::new(Cat { name: String::from("Whiskers") }),
    }
}
```

นี่คือคู่เทียบของ Rust กับการ return interface type จาก TypeScript factory function

## กฎ Object-safety

ไม่ใช่ทุก trait จะทำเป็น trait object ได้ Trait มีความ **object-safe** เมื่อ:

1. ไม่มี generic methods (generics จะต้อง monomorphise ซึ่งทำลาย vtable)
2. Methods ไม่ return `Self` โดยค่า

`Clone` ใน standard library **ไม่** object-safe เพราะ `clone()` return `Self` ส่วน `Display` และ `Debug` เป็น object-safe

<RsOnly title="Static vs dynamic dispatch — เลือกใช้ให้ถูก">

**Static dispatch** (`impl Trait` หรือ `<T: Trait>`) คือค่า default ตัว compiler จะสร้าง machine code เฉพาะสำหรับแต่ละ type — ไม่มี vtable ไม่มี indirection และ inline ได้เต็มที่ เลือกใช้เมื่อรู้ทุก type ตั้งแต่ compile time และต้องการ performance สูงสุด

**Dynamic dispatch** (`dyn Trait`) เพิ่ม pointer indirection หนึ่งชั้นผ่าน vtable ในทุกครั้งที่เรียก method แลกมากับ binary size ที่เล็กลง (มี function เดียวแทนที่จะเป็น N ตัว) และใส่ type ต่างชนิดกันไว้ใน collection เดียวได้ เลือกใช้เมื่อ:

- ต้องการ `Vec` (หรือ collection อื่น) ที่เก็บ concrete type ผสมกันภายใต้ interface เดียว
- Return trait object จากฟังก์ชันและ concrete type ขึ้นอยู่กับ input ขณะ runtime
- สร้าง plugin system หรือ dependency-injection pattern

</RsOnly>

## ลองเล่นดู

export const dynPlayground = `trait Shape {
    fn area(&self) -> f64;
    fn name(&self) -> &str;
}

struct Circle { radius: f64 }
struct Rectangle { width: f64, height: f64 }

impl Shape for Circle {
    fn area(&self) -> f64 { std::f64::consts::PI * self.radius * self.radius }
    fn name(&self) -> &str { "Circle" }
}
impl Shape for Rectangle {
    fn area(&self) -> f64 { self.width * self.height }
    fn name(&self) -> &str { "Rectangle" }
}

fn print_area(shape: &dyn Shape) {
    println!("{}: area = {:.2}", shape.name(), shape.area());
}

fn main() {
    let shapes: Vec<Box<dyn Shape>> = vec![
        Box::new(Circle { radius: 3.0 }),
        Box::new(Rectangle { width: 4.0, height: 5.0 }),
        Box::new(Circle { radius: 1.5 }),
    ];

    for s in &shapes {
        print_area(s.as_ref());
    }

    let total: f64 = shapes.iter().map(|s| s.area()).sum();
    println!("Total area: {:.2}", total);
}`;

<Playground code={dynPlayground} />

export const quizDyn = [
  {
    q: "Trait object ใน Rust คืออะไร?",
    options: [
      "Generic function ที่ถูก monomorphise สำหรับ type เฉพาะ",
      "ค่าที่อยู่หลัง `dyn Trait` pointer ที่รองรับ dynamic dispatch ผ่าน vtable",
      "Compile-time constant ที่ implement trait",
      "Struct ที่ derive ทุก trait อัตโนมัติ"
    ],
    answer: 1
  },
  {
    q: "ทำไม trait objects มักต้องเก็บเป็น `Box<dyn Trait>` แทนที่จะเป็น `dyn Trait` ตรงๆ?",
    options: [
      "Box เร็วกว่า plain reference",
      "Rust ไม่สามารถรู้ขนาดของ trait object ตอน compile time จึงต้องจัดสรรบน heap ผ่าน pointer",
      "Box มี garbage collection อัตโนมัติ",
      "dyn Trait ถูก deprecate แล้ว"
    ],
    answer: 1
  },
  {
    q: "ควรใช้ `&dyn Trait` แทน `<T: Trait>` เมื่อใด?",
    options: [
      "เสมอ เพราะ dynamic dispatch เป็น idiomatic มากกว่า",
      "เมื่อรู้ทุก type ตอน compile time",
      "เมื่อต้องการ collection ที่เก็บ concrete type ผสมกัน หรือ type ถูกกำหนดตอน runtime",
      "เฉพาะเมื่อ trait ไม่ object-safe"
    ],
    answer: 2
  },
  {
    q: "Trait ใดใน standard library ที่ไม่ object-safe?",
    options: [
      "Display",
      "Debug",
      "Clone",
      "Iterator"
    ],
    answer: 2
  }
];

<Quiz client:visible id="advanced/trait-objects" questions={quizDyn} />

<ProgressTracker client:visible id="advanced/trait-objects" />
