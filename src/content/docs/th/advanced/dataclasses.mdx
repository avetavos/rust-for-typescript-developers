---
title: Dataclasses
description: decorator @dataclass ของ Python — __init__, __repr__, __eq__ อัตโนมัติ, frozen records, และ field defaults — เปรียบเทียบกับ TypeScript classes และ interfaces
---
import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import PyOnly from '../../../../components/PyOnly.astro';

## ปัญหาที่ dataclasses แก้ไข

ใน TypeScript คุณมักเขียนคลาสแค่เพื่อเก็บข้อมูลและรับ constructor มาฟรี หรือใช้ `interface` + object literal เมื่อไม่ต้องการเมธอด decorator `@dataclass` ของ Python เชื่อมทั้งสอง: มันคือคลาสที่สร้าง `__init__`, `__repr__`, และ `__eq__` อัตโนมัติจาก field annotations เพื่อให้คุณเขียนแค่ fields

export const basicTs = `// TypeScript — class as data holder
class Point {
  constructor(
    public x: number,
    public y: number
  ) {}

  toString(): string {
    return \`Point(\${this.x}, \${this.y})\`;
  }
}

const p = new Point(3, 4);
console.log(p);          // Point { x: 3, y: 4 }
console.log(p.toString()); // Point(3, 4)`;

export const basicPy = `from dataclasses import dataclass

@dataclass
class Point:
    x: float
    y: float

    def distance_from_origin(self) -> float:
        return (self.x ** 2 + self.y ** 2) ** 0.5

p = Point(3.0, 4.0)
print(p)                          # Point(x=3.0, y=4.0)
print(p.distance_from_origin())   # 5.0

# __eq__ ถูกสร้างอัตโนมัติ
print(Point(1, 2) == Point(1, 2)) # True`;

<TsGo client:visible ts={basicTs} go={basicPy} />

คุณได้ `__init__`, `__repr__`, และ `__eq__` มาฟรีโดยไม่ต้องเขียน boilerplate decorator `@dataclass` อ่าน field annotations ตอน class definition time และสร้างเมธอดเหล่านั้นให้

## Default values และ field()

Default values ทำงานเหมือน parameter defaults ของ TypeScript สำหรับ mutable defaults (lists, dicts) คุณ **ต้อง** ใช้ `field(default_factory=...)` — อย่า assign mutable literal ตรงๆ เพราะทุก instance จะแชร์ออบเจ็กต์เดียวกัน

export const defaultTs = `// TypeScript — parameter defaults
class Config {
  constructor(
    public host: string = "localhost",
    public port: number = 8080,
    public tags: string[] = []  // ⚠ shared ref bug possible
  ) {}
}

// Correct: factory pattern or spread copy
class SafeConfig {
  host: string;
  port: number;
  tags: string[];
  constructor(opts: Partial<SafeConfig> = {}) {
    this.host = opts.host ?? "localhost";
    this.port = opts.port ?? 8080;
    this.tags = [...(opts.tags ?? [])];
  }
}`;

export const defaultPy = `from dataclasses import dataclass, field

@dataclass
class Config:
    host: str = "localhost"
    port: int = 8080
    # field(default_factory=list) ป้องกัน shared-ref bug
    tags: list[str] = field(default_factory=list)

cfg1 = Config()
cfg2 = Config(port=9000, tags=["prod"])

print(cfg1)  # Config(host='localhost', port=8080, tags=[])
print(cfg2)  # Config(host='localhost', port=9000, tags=['prod'])

cfg1.tags.append("dev")
print(cfg1.tags)  # ['dev'] — ไม่กระทบ cfg2
print(cfg2.tags)  # ['prod']`;

<TsGo client:visible ts={defaultTs} go={defaultPy} />

## frozen=True: immutable records

`@dataclass(frozen=True)` ทำให้ fields ทั้งหมดเป็น read-only หลัง `__init__` นี่คือ `Readonly<T>` ของ TypeScript หรือ `const` object ยกเว้นว่า Python enforce จริงที่ runtime

export const frozenTs = `// TypeScript — read-only interface
interface Color {
  readonly r: number;
  readonly g: number;
  readonly b: number;
}

const red: Readonly<{ r: number; g: number; b: number }> = {
  r: 255, g: 0, b: 0
};
// red.r = 100; // TS compile error`;

export const frozenPy = `from dataclasses import dataclass

@dataclass(frozen=True)
class Color:
    r: int
    g: int
    b: int

red = Color(255, 0, 0)
print(red)            # Color(r=255, g=0, b=0)

# frozen=True ทำให้ instance เป็น hashable — ใช้เป็น dict key ได้!
palette = {red: "primary red"}
print(palette[Color(255, 0, 0)])   # primary red

try:
    red.r = 100          # type: ignore
except Exception as e:
    print(f"Error: {e}") # cannot assign to field 'r'`;

<TsGo client:visible ts={frozenTs} go={frozenPy} />

## ลองเล่น

export const dataclassPlaygroundCode = `from dataclasses import dataclass, field

@dataclass
class Point:
    x: float
    y: float

    def distance_from_origin(self) -> float:
        return (self.x ** 2 + self.y ** 2) ** 0.5

@dataclass
class Config:
    host: str = "localhost"
    port: int = 8080
    tags: list[str] = field(default_factory=list)

@dataclass(frozen=True)
class Color:
    r: int
    g: int
    b: int

# Point demo
p = Point(3.0, 4.0)
print(p)
print(f"Distance: {p.distance_from_origin()}")
print(f"Equal: {Point(1, 2) == Point(1, 2)}")

# Config demo
cfg = Config(tags=["web"])
print(cfg)
cfg.tags.append("api")
print(f"Tags: {cfg.tags}")

# Frozen demo
red = Color(255, 0, 0)
print(red)
print(f"Hashable: {hash(red)}")
try:
    red.r = 50          # type: ignore
except Exception as e:
    print(f"Caught: {e}")
`;

<Playground client:visible code={dataclassPlaygroundCode} />

<PyOnly title="post_init และ field metadata">

`@dataclass` มี escape hatches สองอย่างที่ TypeScript ไม่มีคู่เทียบตรงๆ:

**`__post_init__`** — hook ที่รันหลัง `__init__` ที่ถูกสร้าง ใช้สำหรับ derived fields หรือ validation:

```python
from dataclasses import dataclass
import math

@dataclass
class Circle:
    radius: float

    def __post_init__(self) -> None:
        if self.radius <= 0:
            raise ValueError(f"radius must be positive, got {self.radius}")
        self.area: float = math.pi * self.radius ** 2

c = Circle(5.0)
print(c.area)      # 78.53...
Circle(-1)         # ValueError
```

**`field(repr=False, compare=False)`** — ควบคุมละเอียดว่า fields ใดมีส่วนร่วมใน `__repr__` และ `__eq__` มีประโยชน์สำหรับการยกเว้นข้อมูลขนาดใหญ่หรือข้อมูลสำคัญออกจากการเปรียบเทียบ

</PyOnly>

export const quizDataclasses = [
  {
    q: 'ทำไม mutable default values (lists, dicts) ต้องใช้ `field(default_factory=...)` ใน dataclass?',
    options: [
      'เพราะ Python กำหนด syntax นี้',
      'เพื่อป้องกันทุก instance แชร์ mutable object เดียวกัน',
      'เพื่อเปิด frozen mode',
      'เพราะ `__init__` ไม่รับ mutable arguments',
    ],
    answer: 1,
  },
  {
    q: '`@dataclass(frozen=True)` เพิ่มอะไรเมื่อเทียบกับ `@dataclass` ปกติ?',
    options: [
      'ป้องกันการ subclassing',
      'ทำให้ instances เป็น immutable และ hashable',
      'เพิ่ม type-checking ที่ runtime',
      'ทำให้ fields ทั้งหมดเป็น optional',
    ],
    answer: 1,
  },
  {
    q: '`@dataclass` สร้าง dunder methods ใดอัตโนมัติ?',
    options: [
      '__str__, __hash__, __iter__',
      '__init__, __repr__, __eq__',
      '__new__, __del__, __call__',
      '__enter__, __exit__, __len__',
    ],
    answer: 1,
  },
  {
    q: 'จุดประสงค์ของ `__post_init__` ใน dataclass คืออะไร?',
    options: [
      'มันแทนที่ `__init__` ทั้งหมด',
      'มันรันหลัง `__init__` ที่ถูกสร้างสำหรับ derived fields หรือ validation',
      'มันถูกเรียกเมื่อ object ถูก garbage-collect',
      'มันจำเป็นสำหรับ frozen dataclasses',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="advanced/dataclasses" questions={quizDataclasses} />

<ProgressTracker client:visible id="advanced/dataclasses" />
