---
title: Pattern Matching
description: structural pattern matching ด้วย match/case ของ Python 3.10+ — sequence, mapping, และ class patterns — เปรียบเทียบกับ TypeScript switch/case และ destructuring
---
import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import PyOnly from '../../../../components/PyOnly.astro';

## เกินกว่า switch/case

`switch` ของ TypeScript match บนค่าเดียว `match`/`case` ของ Python (introduced ใน Python 3.10) เป็นแบบ **structural** — มัน destructure sequences, dictionaries, และ class instances ได้พร้อมกัน ลองนึกถึงมันว่าคือ `switch` ของ TypeScript + array/object destructuring ที่หลอมรวมเป็น statement เดียว

## Sequence patterns

export const seqTs = `// TypeScript — switch + destructuring
const command = ["move", 10, 20];

if (Array.isArray(command) && command[0] === "move") {
  const [, x, y] = command;
  console.log(\`Move to (\${x}, \${y})\`);
} else if (command[0] === "stop") {
  console.log("Stopping");
} else {
  console.log("Unknown command");
}`;

export const seqPy = `command = ["move", 10, 20]

match command:
    case ["move", x, y]:
        print(f"Move to ({x}, {y})")
    case ["stop"]:
        print("Stopping")
    case _:
        print("Unknown command")

# Output: Move to (10, 20)`;

<TsGo client:visible ts={seqTs} go={seqPy} />

pattern `case ["move", x, y]` ตรวจสอบความยาว list ตรวจสอบว่า element แรกเท่ากับ `"move"` และ **bind** `x` และ `y` กับ element ที่สองและสาม — ทั้งหมดในขั้นตอนเดียว

## Mapping patterns

Mapping patterns match dictionary shapes คล้ายกับ TypeScript object destructuring ใน switch

export const mapTs = `// TypeScript — discriminated union pattern
type Event =
  | { type: "click"; x: number; y: number }
  | { type: "keypress"; key: string };

function handle(event: Event) {
  switch (event.type) {
    case "click":
      console.log(\`Click at (\${event.x}, \${event.y})\`);
      break;
    case "keypress":
      console.log(\`Key: \${event.key}\`);
      break;
  }
}`;

export const mapPy = `event = {"type": "click", "x": 100, "y": 200}

match event:
    case {"type": "click", "x": x, "y": y}:
        print(f"Click at ({x}, {y})")
    case {"type": "keypress", "key": key}:
        print(f"Key pressed: {key}")
    case _:
        print("Unknown event")

# Output: Click at (100, 200)`;

<TsGo client:visible ts={mapTs} go={mapPy} />

## Class patterns

Class patterns destructure dataclass (หรือคลาสใดก็ตามที่มี `__match_args__`) instances

export const classTs = `// TypeScript — instanceof + destructuring
class Point { constructor(public x: number, public y: number) {} }

function describe(p: Point): string {
  if (p.x === 0 && p.y === 0) return "Origin";
  if (p.x === 0) return \`Y-axis at \${p.y}\`;
  if (p.y === 0) return \`X-axis at \${p.x}\`;
  return \`At (\${p.x}, \${p.y})\`;
}`;

export const classPy = `from dataclasses import dataclass

@dataclass
class Point:
    x: int
    y: int

point = Point(0, 5)

match point:
    case Point(x=0, y=0):
        print("Origin")
    case Point(x=0, y=y):
        print(f"On Y-axis at {y}")
    case Point(x=x, y=0):
        print(f"On X-axis at {x}")
    case Point(x=x, y=y):
        print(f"At ({x}, {y})")

# Output: On Y-axis at 5`;

<TsGo client:visible ts={classTs} go={classPy} />

## Guard conditions (if clauses)

คุณเพิ่ม `if` guard ใน case arm ใดก็ได้ — เทียบเท่ากับ conditional switch trick ของ TypeScript

export const guardTs = `// TypeScript — ไม่มี guard syntax ที่สะอาดใน switch
function grade(score: number): string {
  if (score >= 90) return "A";
  if (score >= 80) return "B";
  if (score >= 70) return "C";
  return "F";
}`;

export const guardPy = `def grade(score: int) -> str:
    match score:
        case s if s >= 90:
            return "A"
        case s if s >= 80:
            return "B"
        case s if s >= 70:
            return "C"
        case _:
            return "F"

print(grade(85))   # B`;

<TsGo client:visible ts={guardTs} go={guardPy} />

## ลองเล่น

export const patternMatchingPlaygroundCode = `from dataclasses import dataclass

@dataclass
class Point:
    x: int
    y: int

# Sequence pattern
for cmd in [["move", 5, 10], ["stop"], ["jump", 3], ["move", 0, 0]]:
    match cmd:
        case ["move", x, y]:
            print(f"  move -> ({x}, {y})")
        case ["stop"]:
            print("  stop")
        case _:
            print(f"  unknown: {cmd}")

print()

# Mapping pattern
events = [
    {"type": "click", "x": 10, "y": 20},
    {"type": "keypress", "key": "Enter"},
    {"type": "resize", "w": 800, "h": 600},
]
for evt in events:
    match evt:
        case {"type": "click", "x": x, "y": y}:
            print(f"  click ({x},{y})")
        case {"type": "keypress", "key": k}:
            print(f"  key: {k}")
        case {"type": t}:
            print(f"  other event: {t}")

print()

# Class pattern with guards
for pt in [Point(0, 0), Point(0, 7), Point(3, 0), Point(3, 4)]:
    match pt:
        case Point(x=0, y=0):
            print("  origin")
        case Point(x=0, y=y) if y > 0:
            print(f"  +Y axis at {y}")
        case Point(x=x, y=0) if x > 0:
            print(f"  +X axis at {x}")
        case Point(x=x, y=y):
            print(f"  quadrant ({x},{y})")
`;

<Playground client:visible code={patternMatchingPlaygroundCode} />

<PyOnly title="OR patterns และ AS patterns">

`match` ของ Python มี pattern forms พิเศษสองอย่างที่ TypeScript switch/destructuring ไม่สามารถแสดงออกได้:

**OR patterns** — match หลาย structures ใน arm เดียว:

```python
command = ["quit"]
match command:
    case ["quit"] | ["exit"] | ["q"]:
        print("Exiting...")
    case _:
        print("Continue")
```

**AS patterns** — bind ทั้ง match พร้อมตรวจสอบ structure:

```python
match command:
    case [action, *args] as full_command:
        print(f"Action: {action}, Full: {full_command}")
```

สิ่งเหล่านี้ทำให้ `match`/`case` มีความสามารถในการแสดงออกได้มากกว่า switch statement ใดๆ อย่างมีนัยสำคัญ

</PyOnly>

export const quizPatternMatching = [
  {
    q: 'Python เวอร์ชันใดที่ introduce structural pattern matching?',
    options: ['Python 3.8', 'Python 3.9', 'Python 3.10', 'Python 3.12'],
    answer: 2,
  },
  {
    q: 'ใน `case ["move", x, y]` `x` ทำอะไร?',
    options: [
      'ตรวจสอบว่า element ที่สองเท่ากับตัวแปร x',
      'bind element ที่สองกับตัวแปรใหม่ชื่อ x',
      'เป็น wildcard ที่ไม่สนใจ element ที่สอง',
      'ตรวจสอบว่า x เป็น number',
    ],
    answer: 1,
  },
  {
    q: 'จะเพิ่ม conditional check ใน case arm ได้อย่างไร?',
    options: [
      'case x when x > 0:',
      'case x if x > 0:',
      'case x and x > 0:',
      'case x, guard=x > 0:',
    ],
    answer: 1,
  },
  {
    q: '`case _:` match อะไร?',
    options: [
      'เฉพาะ None values',
      'เฉพาะ sequences ว่าง',
      'ทุกอย่าง — มันคือ wildcard / default arm',
      'เฉพาะ strings',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="advanced/pattern-matching" questions={quizPatternMatching} />

<ProgressTracker client:visible id="advanced/pattern-matching" />
