---
title: 'Testing — #[test], cargo test และ Doctest'
description: 'Unit test ด้วย #[test], assert_eq!, cargo test และ doctest ใน Rust เทียบกับ Jest'
---

import TsGo from '../../../../components/TsGo.astro';
import Playground from '../../../../components/Playground.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import RsOnly from '../../../../components/RsOnly.astro';
import Mermaid from '../../../../components/Mermaid.astro';

export const integrationTestTree = `flowchart TD
  root["my-lib/"] --> src["src/"]
  src --> lib["lib.rs"]
  root --> tests["tests/"]
  tests --> it["integration_test.rs — cargo test finds these automatically"]`;

## Jest → cargo test

ใน TypeScript คุณใช้ Jest (หรือ Vitest) เขียน `describe`/`it`/`expect` ไว้ใน `.test.ts` แยกไฟล์ ส่วน Rust มี test runner ติดมากับ compiler อยู่แล้ว: แปะ `#[test]` บน function ไหนก็ได้ แล้ว `cargo test` จะไล่หาและรันให้เองทั้งหมด — ไม่ต้องพึ่ง crate ภายนอก

## Unit test อยู่ใกล้ code

export const testingTs = `// math.ts
export function add(a: number, b: number): number {
  return a + b;
}

// math.test.ts
import { add } from './math';
test('adds two numbers', () => {
  expect(add(3, 4)).toBe(7);
});`;

export const testingRs = `// src/lib.rs  (test อยู่ใน file เดียวกัน)
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

#[cfg(test)]
mod tests {
    use super::*;  // import ทุกอย่างจาก parent module

    #[test]
    fn test_add() {
        assert_eq!(add(3, 4), 7);
    }

    #[test]
    fn test_add_negative() {
        assert_eq!(add(-1, 1), 0);
    }
}`;

<TsGo ts={testingTs} go={testingRs} />

attribute `#[cfg(test)]` หมายความว่า test module จะ compile เฉพาะตอนรัน `cargo test` เท่านั้น — release build จะตัดทิ้งให้อัตโนมัติ

## การรัน test

```bash
cargo test                    # run all tests in the project
cargo test test_add           # run tests whose name contains "test_add"
cargo test -- --nocapture     # show println! output during tests
cargo test -- --test-threads=1  # run tests sequentially (no parallelism)
```

## Assertion macro ที่ใช้บ่อย

```rust
// @skip-verify nocode: placeholder identifiers (left/right/condition), illustrating macro signatures
assert_eq!(left, right);         // expect(a).toBe(b)
assert_ne!(left, right);         // expect(a).not.toBe(b)
assert!(condition);              // expect(condition).toBeTruthy()
assert!(condition, "message {}",  42);  // with a custom failure message
```

## การคาดหวัง panic (เหมือน Jest's toThrow)

```rust
fn divide(a: i32, b: i32) -> i32 {
    a / b
}

#[test]
#[should_panic(expected = "divide by zero")]
fn test_divide_by_zero() {
    divide(10, 0);  // this function should panic
}
```

## Doctest — test ใน doc comment

Rust รัน code example ที่เขียนใน `///` doc comment ได้จริง ตัวอย่างชุดเดียวจึงทำหน้าที่เป็นทั้ง documentation และ test

```rust
/// Adds two integers together.
///
/// # Examples
///
///     let result = my_crate::add(2, 3);
///     assert_eq!(result, 5);
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}
```

รันด้วย command เดิม: `cargo test`

> Playground note: function `#[test]` ต้องการ context ของ `cargo test` binary และไม่สามารถรันใน browser playground ได้ snippet ด้านล่างใช้ `fn main` กับ `assert_eq!` เพื่อสาธิต logic เดียวกัน — รัน `cargo test` ใน local สำหรับประสบการณ์ test suite เต็มรูปแบบ

## ลองเลย — logic ที่จะ test

export const testingCode = `fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn is_even(n: i32) -> bool {
    n % 2 == 0
}

fn main() {
    // Manually exercise the logic (in real projects, use #[test])
    assert_eq!(add(3, 4), 7);
    assert_eq!(add(-1, 1), 0);
    assert!(is_even(4));
    assert!(!is_even(7));
    println!("All assertions passed!");
}`;

<Playground code={testingCode} />

<RsOnly title="Integration test คืออะไร">

Integration test อยู่ใน directory `tests/` ระดับบนสุด (ไม่ใช่ใน `src/`) แต่ละ `.rs` file ใน `tests/` จะถูก compile เป็น crate แยกที่ import library ของคุณ — สามารถ access เฉพาะ public API เท่านั้น เหมือนกับ consumer ภายนอก

<Mermaid code={integrationTestTree} title="โครงสร้าง integration test" />

```rust
// @skip-verify multi-file: separate tests/ crate importing the library crate `my_lib`
// tests/integration_test.rs
use my_lib::add;

#[test]
fn test_add_from_outside() {
    assert_eq!(add(10, 20), 30);
}
```

</RsOnly>

export const quizTesting = [
  {
    q: "Attribute ใดทำเครื่องหมาย Rust function ว่าเป็น test?",
    options: [
      "#[it]",
      "#[test]",
      "#[spec]",
      "#[should_run]"
    ],
    answer: 1
  },
  {
    q: "`#[cfg(test)]` ทำอะไร?",
    options: [
      "กำหนด setting ของ test runner",
      "ให้แน่ใจว่า module ถูก compile เฉพาะเมื่อรัน cargo test",
      "ทำเครื่องหมาย module ว่ามี integration test",
      "ปิด optimisation ระหว่าง testing"
    ],
    answer: 1
  },
  {
    q: "Macro ใดเทียบเท่ากับ `expect(a).toBe(b)` ของ Jest?",
    options: [
      "assert!(a == b)",
      "check_eq!(a, b)",
      "assert_eq!(a, b)",
      "expect_eq!(a, b)"
    ],
    answer: 2
  },
  {
    q: "Rust integration test อยู่ที่ไหน?",
    options: [
      "ใน src/ ข้างๆ source file",
      "ใน directory tests/ ที่ project root",
      "ใน __tests__/ ภายใน src/",
      "ใน Cargo.toml ภายใต้ [test]"
    ],
    answer: 1
  }
];

<Quiz client:visible id="tooling/testing" questions={quizTesting} />

<ProgressTracker client:visible id="tooling/testing" />
